<?xml version="1.0" encoding="utf-8"?>
<cruise xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="cruise-config.xsd" schemaVersion="75">
  <server artifactsdir="artifacts" agentAutoRegisterKey="123456789abcdef" commandRepositoryLocation="default" />
  <templates>
    <pipeline name="docker">
      <stage name="build" cleanWorkingDir="true">
        <jobs>
          <job name="build">
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>docker login --email $DOCKER_EMAIL --username $DOCKER_USERNAME --password $DOCKER_PASSWORD &amp;&amp; docker build --tag "$DOCKER_USERNAME/$GO_PIPELINE_NAME:$GO_REVISION" . &amp;&amp; docker push "$DOCKER_USERNAME/$GO_PIPELINE_NAME:$GO_REVISION"</arg>
                <runif status="passed" />
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
      <stage name="test" cleanWorkingDir="true">
        <jobs>
          <job name="test">
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>docker login --email $DOCKER_EMAIL --username $DOCKER_USERNAME --password $DOCKER_PASSWORD &amp;&amp; docker run "$DOCKER_USERNAME/$GO_PIPELINE_NAME:$GO_REVISION" test</arg>
                <runif status="passed" />
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
      <stage name="push" cleanWorkingDir="true">
        <jobs>
          <job name="push">
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>docker login --email $DOCKER_EMAIL --username $DOCKER_USERNAME --password $DOCKER_PASSWORD &amp;&amp; docker pull "$DOCKER_USERNAME/$GO_PIPELINE_NAME:$GO_REVISION" &amp;&amp; docker tag --force "$DOCKER_USERNAME/$GO_PIPELINE_NAME:$GO_REVISION" "$DOCKER_USERNAME/$GO_PIPELINE_NAME:latest" &amp;&amp; docker push "$DOCKER_USERNAME/$GO_PIPELINE_NAME:latest"</arg>
                <runif status="passed" />
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
      <stage name="deploy" cleanWorkingDir="true">
        <jobs>
          <job name="deploy">
            <tasks>
              <exec command="bash">
                <arg>-c</arg>
                <arg>docker login --email $DOCKER_EMAIL --username $DOCKER_USERNAME --password $DOCKER_PASSWORD &amp;&amp; docker run "$DOCKER_USERNAME/$GO_PIPELINE_NAME:$GO_REVISION" deploy</arg>
                <runif status="passed" />
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>
  </templates>
</cruise>
