go-server:
  build: ./gocd-docker-server
  volumes:
    - ./gocd-docker-server/go:/etc/go
  ports:
    - '8153:8153'
go-agent-1:
  build: ./gocd-docker-agent
  privileged: true
  links:
    - go-server
go-agent-2:
  build: ./gocd-docker-agent
  privileged: true
  links:
    - go-server
