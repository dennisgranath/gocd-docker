server-data:
  build: ./server-data
server:
  image: gocd/gocd-server
  hostname: go-server
  ports:
    - '8153:8153'
agent:
  build: ./agent
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    - $DOCKER_BINARY:/bin/docker
  links:
    - server
