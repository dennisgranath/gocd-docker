server:
  image: gocd/gocd-server
  volumes:
    - './server/init.sh:/init.sh'
    - './server/go:/etc/go'
  hostname: go-server
  ports:
    - '8153:8153'
agent:
  build: ./agent
  privileged: true
  links:
    - server
